# FastAPI RAG Tutorial - Environment Variables
# Copy this file to .env and update with your actual values

# ============================================
# Project Information
# ============================================
# These are optional and have defaults in the code
# PROJECT_NAME=FastAPI RAG Tutorial
# VERSION=0.1.0
# DESCRIPTION=A step-by-step FastAPI RAG tutorial project

# ============================================
# Logging Configuration
# ============================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Path to log file (leave empty for console only)
# Example: logs/app.log
LOG_FILE=

# ============================================
# OpenAI Configuration (for Embeddings and LLM)
# ============================================
# Required if you want to use OpenAI embeddings or LLM
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ============================================
# GROQ Configuration (for LLM)
# ============================================
# Optional: Required if you want to use GROQ LLM
# Get your API key from: https://console.groq.com/keys
# GROQ_API_KEY=your_groq_api_key_here

# ============================================
# ChromaDB Configuration (for Vector Store)
# ============================================
# Optional: Custom path for ChromaDB persistence directory
# Default: ./chroma_db (in project root)
# CHROMA_DB_PATH=./chroma_db

# ============================================
# AWS S3 Configuration (for File Storage)
# ============================================
# Optional: If configured, raw files will be stored in S3
# If not configured, files will be stored locally in ./storage/
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1
# AWS_S3_PREFIX=rag-documents

# ============================================
# Local Storage Configuration
# ============================================
# Optional: Custom path for local file storage
# Default: ./storage (in project root)
# Used if S3 is not configured
# LOCAL_STORAGE_PATH=./storage

# ============================================
# Notes
# ============================================
# 1. Never commit the .env file to version control
# 2. The .env file is already in .gitignore
# 3. For production, use environment variables or a secrets manager
# 4. OpenAI API key is optional - HuggingFace embeddings work without it
